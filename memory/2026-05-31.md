# 2026-05-31 - Deela - Session 2

## Project URLs
- Frontend (Vercel): https://deela-two.vercel.app
- Backend (Render): https://deela-foa0.onrender.com
- GitHub: github.com/luminextech-labs/deela
- Repo local: /Users/adrenaline/deela/

## Backend Structure (local)
- `/Users/adrenaline/deela/backend/app/routes/trending.py` - **edited with logging**
- `/Users/adrenaline/deela/backend/app/routes/products.py` - **broken: prices empty**
- `/Users/adrenaline/deela/backend/app/routes/admin.py` - works (uses urllib + ssl)
- `/Users/adrenaline/deela/backend/app/routes/products_rest.py` - v2 REST version

## Supabase
- Project: dtdkjtqwnwqvozkayeps (different from earlier!)
- Service key set on Render: SUPABASE_SERVICE_KEY
- Direct REST API works: `curl "https://dtdkjtqwnwqvozkayeps.supabase.co/rest/v1/products?select=id,name,prices(price,...)"` returns data correctly

## Critical Bug
- `/api/products/` returns 20 products but `prices: []` (empty)
- `/api/trending/deals` returns `[]` because products have no prices
- Root cause: products.py uses SQLAlchemy → direct PG connection which loads products but NOT related prices (lazy load issue)
- admin.py works because it uses urllib + Supabase REST API directly with embed

## Fix Plan
1. Edit `/Users/adrenaline/deela/backend/app/routes/products.py`:
   - Change `_list_via_rest` to use same urllib+ssl approach as admin.py (bypasses DNS/connection issues)
   - Or change to use `products_rest.py` style with httpx but without http2

2. Redeploy on Render (git push triggers auto-deploy)

## Status
- Backend code pushed: e1beb6b, 29186bb
- trending.py has traceback logging added
- products.py needs fix (prices embedded query fails)
# 2026-05-31 - Deela - Session 2 (continued)

## Railway Deployment Failing

**Problem:** Railway deploy fails at Docker build step. Error:
```
COPY failed: file not found in build context: 'backend/Dockerfile'
```

**Root cause:** Root Directory set to `/backend` on Railway dashboard. When Railway runs `docker build -f backend/Dockerfile .` it looks for `backend/backend/Dockerfile` which doesn't exist.

**Attempted fixes:**
- Created `plan.toml` in repo root and backend/ folder
- Created `railway.json` with build config
- Railway ignores plan.toml for custom builds

**Current state:** Railway deployment at commit `e193c72` with "Active" status but 502 error (the broken first commit). Backend service `thorough-clarity` shows "Failed to build"

## Alternative: Deploy to Contabo VPS

Given Railway issues, best option is to deploy Deela backend to Contabo VPS (same machine as MindTrade OS).

**VPS Info:**
- Host: 185.230.138.51
- SSH: available (same as MindTrade)
- Existing MindTrade at /opt/mindtrade/

**Plan:**
1. Create `/opt/deela/` on Contabo
2. Clone repo or rsync code
3. Install dependencies with pip
4. Run with uvicorn
5. Configure nginx reverse proxy (optional, or run on different port)
6. Point frontend to new backend URL

## Supabase Config
- Project ID: dtdkjtqwnwqvozkayeps
- Service Role Key: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImR0ZGtqdHF3bndxdm96a2F5ZXBzIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc3OTk2MTY5MywiZXhwIjoyMDk1NTM3NjkzfQ.j2lPuJPiPvhUKd3LzQpD9G38--2Xr2qxsESqA8eH0sM

## Next Action
- SSH to Contabo and deploy Deela backend directly
- Use `nohup uvicorn app.main:app --host 0.0.0.0 --port 8001 &` for background
- Update frontend Vercel env to point to new backend